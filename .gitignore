# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dev-dist
*.local

# Environment / secrets
.env
.env.*
!.env.example
!.env.capacitor

# Local npm registry override
.npmrc

# Android release signing — NEVER commit the keystore or its passwords
android/keystore.properties
*.jks
*.keystore

# Vite transient config-load artifacts
vite.config.*.timestamp-*.mjs

# Database backups / dumps
*.backup
*.backup.gz
*.dump
*.sql.gz

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local (per-user) settings
.claude/settings.local.json

# Local MCP server config (per-developer project ref) — copy from .mcp.json.example
.mcp.json
